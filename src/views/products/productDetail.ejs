<%- include('../partials/metadata') %>
    <%- include('../partials/header') %>
        <main>
            <div class="padre-contenedor">
                <div class="container">
                    <div class="div-foto">
                        <div class="div-contenedor">
                            <img class="img-grande" src="/images/products/<%= producto.img1 %>" alt="">
                        </div>
                        <div class="div-contenedor-img-principal">
                            <img class="img-chica " src="/images/products/<%= producto.img1 %>" alt="">
                        </div>
                    </div>
                </div>
                <div class="container-1">
                    <div class="div-detalle">
                        <div class="detalle-titulo">
                            <h1>
                                <%= producto.name %>
                            </h1>
                        </div>
                        <div class="price">
                            <% if (producto.discount==0) { %>
                                <p class="precio-producto">
                                    $<%= toThousand(producto.price) %>
                                </p>
                                <% } else { %>
                                    <p class="precio-producto">$<%= toThousand(producto.price-(producto.price *
                                            producto.discount /100))%> &nbsp
                                            <FONT SIZE=5 style="color:#009900" ;>
                                                <%= producto.discount %>% Off
                                            </font>
                                    </p>
                                    <% } %>

                        </div>
                        <div class="cuotas">
                            <p class="price"> 3 cuotas sin interés de
                                <% if (producto.discount==0) { %>
                                    $<%= toThousand(producto.price/3) %>
                                        <% } else { %>
                                            $<%= toThousand((producto.price-(producto.price * producto.discount
                                                /100))/3) %>
                                                <% } %>
                            </p>
                        </div>
                        <div class="medios-pagos">
                            <a class="a-pagos" href="#">ver medios de pagos</a>
                        </div>
                        <div class="form-color" action="">
                            <label class="label-color " for="color">Color:</label>
                            <input type="color" id="color" name="color">
                        </div>
                        <div class="talles">
                            <p class="talle">Talles:<%= producto.size %>
                            </p>
                        </div>
                        <div class="cantidad">
                            <label for="cantidad">Cantidad: </label>
                            <input class="input" type="number" id="cantidad" name="cantidad">
                        </div>
                        <input type="submit" class="boton-agregar-carrito"> 
                        
                        <div class="button-send">
                            <a href="/products/edit/<%= producto.id%>" id="input-editar">
                                <button type="submit" class="button-div">
                                    Editar producto</button></a>
    
                            <form action="/products/delete/<%= producto.id %>?_method=DELETE" method="POST">
                                <button type="submit" class="button-div">Eliminar producto</button>
                            </form>
                            <div class="agregar-carrito">
                                <a href="/products/carrito/<%= producto.id %> ">
                                    <button type="submit" class="button-div">Agregar al carrito</button>
                                </a>
                            </div>
                        </div>
                        
                    </div>

                </div>
            </div>
        </main>
        <%- include('../partials/footer') %>